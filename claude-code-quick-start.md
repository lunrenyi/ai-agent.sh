# Claude Code 快速入门指南

> 基于两篇实战文章整理的核心工作流，包含具体步骤和实践示例

---

## 一句话总结

> **先深入阅读，再写计划并注解到正确，最后让 Claude 全部执行。**

---

## 准备工作

### 1. 配置 CLAUDE.md

在项目根目录创建 `CLAUDE.md`（注意是文件名，不是 CLAUDE.md）：

```markdown
# 项目名称

> 简短的项目描述

---

## 项目结构

简要说明目录结构

---

## 工作流规范

- 核心原则：先思考再打字
- 标准流程：研究 → 规划 → 注解循环 → 实现
- 研究产出写入 research.md
- 计划写入 plan.md

---

## 代码规范

具体的代码规范，如：
- 使用 TypeScript strict mode
- 遵循项目已有的代码风格
- 测试要求等
```

> **CLAUDE.md 是你最大的杠杆点**。保持简短（150-200 条指令）、具体、说原因。

### 2. 了解基本命令

| 命令 | 用途 |
|------|------|
| `/clear` | 清除对话，Claude 仍记得 CLAUDE.md |
| `/compact` | 压缩上下文获取摘要 |
| `Shift + Tab` | 切换到 Sonnet 模型（用于实施） |

---

## 核心工作流

```
研究 → 规划 → 注解循环 → 实现
```

---

### 阶段一：研究

在任何代码修改之前，先让 Claude 彻底理解代码库。

#### 具体步骤

1. **打开 Claude Code**，进入项目目录
2. **发送研究指令**
3. **等待 research.md 生成**
4. **阅读研究报告**，确认 Claude 理解了

#### 研究指令模板

```
"深入阅读这个文件夹，深刻理解它的工作原理。
完成后，将你的发现写入 research.md"

"详细研究 [模块名]，理解其架构和数据流，
完成后，将发现写入 research.md"

"研究 [功能名] 的实现，寻找潜在的 bug。
一直研究直到找到所有 bug。完成后，将发现写入 research.md"
```

#### 关键要点

| 要点 | 说明 |
|------|------|
| 使用强语气词 | "deeply"、"intricacies"、"thoroughly"，否则 Claude 会 skim |
| 必须有书面产物 | research.md 是审查的基础 |
| 研究错误 → 规划错误 → 实现错误 | 最大的失败是破坏周围系统 |

---

### 阶段二：规划

审查研究后，请求详细的实现计划。

#### 具体步骤

1. **阅读 research.md**，确认理解正确
2. **发送规划指令**
3. **等待 plan.md 生成**
4. **审查计划**，检查是否有遗漏

#### 规划指令模板

```
"我想构建 [功能名称]，写一份详细的 plan.md，
列出如何实现，包括代码片段和将被修改的文件路径"

"[功能名] 应该支持 [具体需求]，写一份详细的 plan.md。
在提出更改之前先阅读源文件，基于实际代码库制定计划"

"基于 research.md 中的发现，编写修复 [问题] 的详细计划"
```

#### 计划应包含

- 实现方法的详细解释
- 代码片段（显示实际更改）
- 将被修改的文件路径
- 注意事项和权衡
- 待办事项列表（粒度任务分解）

---

### 阶段三：注解循环（核心）

这是工作流中最独特的部分——在计划文档中添加内联注释，迭代完善。

#### 具体步骤

1. **Claude 写完 plan.md**
2. **你在编辑器中打开 plan.md**
3. **添加内联注释**（纠正、删除、简化）
4. **发送注释更新指令**
5. **重复直到满意**
6. **请求待办事项列表**

#### 注释类型与示例

| 类型 | 示例 |
|------|------|
| 技术纠正 | "使用 drizzle:generate 迁移，不要原始 SQL" |
| API 纠正 | "不——这应该是 PATCH，不是 PUT" |
| 功能删除 | "完全删除这部分，我们不需要缓存" |
| 简化 | "队列消费者已经处理重试，所以这个重试逻辑是多余的，删除它" |
| 逻辑纠正 | "可见性字段需要在列表本身而不是单个项目上" |
| 约束添加 | "函数签名不应该改变，调用者应该适应，而不是库" |

#### 关键命令

> "我在文档中添加了一些注释，解决所有注释并更新文档。**先不要实现**"

> [!IMPORTANT]
> 一定要说 "先不要实现"，否则 Claude 会跳到代码。

#### 注释更新指令

```
"在计划中添加详细的待办事项列表，列出完成计划所需的所有阶段和个别任务。
**先不要实现**"

"解决文档中的所有注释，并相应更新文档。**先不要实现**"
```

---

### 阶段四：实现

当计划准备好后，发出实现命令。

#### 具体步骤

1. **确认 plan.md 中的注释都已解决**
2. **确认待办事项列表完整**
3. **发送实现指令**
4. **Claude 执行计划**
5. **持续验证**（类型检查、测试）

#### 实现指令模板

```
"全部实现。任务完成后在 plan.md 中标记为完成。
不要添加不必要的注释，不要使用 any 类型。
持续运行类型检查确保没有引入问题。"

"开始实施 plan.md 中的待办事项。不要停止，直到所有任务都完成。
完成后在计划中标记为完成。"
```

#### 验证命令（根据项目类型）

```bash
# TypeScript 项目
npm run type-check
npm run lint

# Python 项目
python -m mypy .
flake8 .

# 测试
npm test
pytest
```

---

## 关键原则详解

### 1. 先思考再打字

| 做法 | 说明 |
|------|------|
| 按两次 `Shift + Tab` | 进入规划模式 |
| 规划产生的结果远优于直接开干 | 10 次中有 10 次，规划模式下的输出明显更好 |
| 写 plan.md 再实现 | 创意工作在规划中，不在编码中 |

### 2. 使用 CLAUDE.md

| 原则 | 说明 |
|------|------|
| 保持简短 | 150-200 条指令足够 |
| 特定于项目 | 解释奇怪的命令和行为 |
| 说原因 | "Use strict mode because we've had production bugs" |
| 持续更新 | 每次纠正同样问题两次就记录进去 |

### 3. 上下文管理

- 上下文到 **30% 质量就开始退化**，不是等到 100%
- **作用域对话**：一个对话一个功能
- 复杂工作时写入外部文件（plan.md）
- 不怕 `/clear`：Claude 仍有你的 CLAUDE.md

### 4. Prompting 技巧

| 做法 | 示例 |
|------|------|
| 具体 | "Build auth" → "Build email/password auth using User model, store sessions in Redis" |
| 说不要什么 | "Keep simple. Don't add abstractions. One file if possible." |
| 给背景 | "We need this fast because it runs on every request" |

> **输出 = 输入**。如果输出烂，输入就烂。

---

## 常见问题与解决策略

### 卡住时怎么办

| 策略 | 说明 |
|------|------|
| **简单开始** | `/clear` 清除对话，累积的上下文可能让它困惑 |
| **简化任务** | 拆成小块，不要一次让 Claude 做太多 |
| **展示例子** | 写一个最小例子："输出应该像这样" |
| **换个角度** | 你框定问题的方式可能和 Claude 的思考方式不匹配 |

> 如果解释了同样的事三次 Claude 还不明白，解释更多没帮助。改变点什么。

### 常见陷阱

1. **跳过研究直接规划** —— 没有理解代码，规划就是盲人摸象
2. **跳过注解直接实现** —— 省的注释时间，会在调试中偿还
3. **一次让 Claude 做太多** —— 拆成小块更可靠
4. **输入模糊** —— "Build auth" vs "Build email/password auth using..."

---

## 进阶技巧

### 模型选择

| 模型 | 适用场景 |
|------|----------|
| **Sonnet** | 更快更便宜，适合路径清晰的任务（写样板、根据计划重构） |
| **Opus** | 更慢更贵，适合复杂推理、规划、深入思考 |

**推荐**：用 Opus 规划和做架构决策，然后切换到 Sonnet（`Shift + Tab`）进行实施。

### MCP (Model Context Protocol)

让 Claude 连接到外部服务：Slack、GitHub、数据库、API。

> 如果你经常把信息从一处复制到 Claude，很可能有 MCP 能自动做这件事。

### Hooks

在 Claude 更改前后自动运行代码：
- 每次编辑后运行 Prettier
- 每次编辑后运行类型检查

### 自定义斜杠命令

1. 创建 `.claude/commands` 文件夹
2. 添加 markdown 文件写你的 prompts
3. 现在可以用 `/commandname` 运行

---

## 总结与行动清单

### 核心原则

1. **永远不要让 Claude 写代码，直到你批准了书面计划**
2. **研究 → 规划 → 注解 → 实现** 是核心管道
3. **CLAUDE.md** 是最大的杠杆点
4. **上下文 30% 就退化**，不怕 clear
5. **输出来自输入**，保持 prompt 具体
6. **卡住时改变方法**，不要循环

### 行动清单

- [ ] 为项目创建 CLAUDE.md
- [ ] 用下一个任务练习核心工作流
- [ ] 尝试 `/clear` 和 `/compact`
- [ ] 配置一个 Hook（类型检查或格式化）
- [ ] 创建第一个自定义斜杠命令

---

*整理自 Boris Tone 的实战心得*
